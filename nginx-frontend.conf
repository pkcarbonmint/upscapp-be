server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html student_intake_wizard.html;

    # Enable gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml+rss
        application/json;

    # Static assets with long-term caching
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Vary Accept-Encoding;
    }

    # HTML files with short-term caching
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
        add_header Vary Accept-Encoding;
    }

    # API proxy to Python backend (must come before root location)
    location /api/ {
        # Use resolver for dynamic upstream resolution
        resolver 127.0.0.11 valid=30s;
        set $upstream_app app;
        proxy_pass http://$upstream_app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Handle connection errors gracefully
        proxy_connect_timeout 1s;
        proxy_send_timeout 1s;
        proxy_read_timeout 1s;
        
        # Handle upstream errors gracefully
        proxy_intercept_errors on;
        error_page 502 503 504 = @backend_unavailable;
    }
    
    # Fallback when backend is unavailable
    location @backend_unavailable {
        return 503 '{"error": "Backend service unavailable"}';
        add_header Content-Type application/json;
    }

    # Health check
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }

    # Specific application entry points
    location /onboarding {
        try_files /onboarding.html =404;
    }
    
    location /studyplanner {
        try_files /studyplanner.html =404;
    }
    
    location /mentorship {
        try_files /mentorship.html =404;
    }
    
    location /onboarding-app {
        try_files /onboarding-app.html =404;
    }

    # Main application routes (must come last)
    location / {
        try_files $uri $uri/ /index.html;
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }
}
